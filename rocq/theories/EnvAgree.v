(** * EnvAgree.v

    Global-environment agreement: constant/inductive lookup on the Lean side
    matches [EGlobalEnv] lookup ([lookup_env], [lookup_constant],
    [lookup_inductive]) under the environment translation. *)
